# テスト結果と改善案（2026-05-01）

## 1. テスト結果（成功）

- **ブラウザ会話テスト**: 成功
  - マルチターンが成立
  - RAG出典の表示ができている（`/chat` 経路）
- **サーバー起動**: 成功
  - Neo4j接続 / Retriever作成 / LLM初期化 / RAGチェーン構築が成功

## 2. ログ所見

- Flask開発サーバの警告が出る（本番はWSGIサーバ推奨）
- `GET /favicon.ico 404` が出る（機能影響なし）
- 応答時間ログ（完了まで）が長く出る場合があるため、**TTFB（最初のチャンク）** と **完了時間** を分けて計測するのが有効

## 3. 改善案（優先度順）

### P0: ストリーミング経路の標準化（TTFBを最大限削減）

- `/chat_stream` を優先し、UIは逐次表示
- 失敗時は `/chat` へフォールバック（出典付き表示を維持）

### P1: 計測ログを「TTFB」と「完了時間」に分割

- `time_to_first_chunk` と `time_to_complete` を区別してログ化
- ストリーミング導入効果を定量で説明できる

### P2: 見た目/ノイズ改善

- `favicon.ico 404` を解消（任意）

